name: Integraci√≥n Continua - Hola Mundo

on:
  push:
    branches:
      - main

jobs:
  hola-mundo-ci:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Ejecutar Hola Mundo
        run: node index.js

      - name: Enviar notificaci√≥n a ntfy
        run: |
          curl -X POST \
          -H "Title: Ì∫Ä Push a main" \
          -H "Tags: github,ci,devops,itla" \
          -H "Priority: 3" \
          -d "Se realiz√≥ un push al branch main y el workflow se
